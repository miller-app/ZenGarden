PLATFORM_TARGETS=libzengarden libzengarden-static

ifeq ($(DEBUG),1)
	CXXFLAGS = -g -lpthread -std=gnu++11 -Wall -I$(my-dir)
else
	CXXFLAGS = -O3 -lpthread -std=gnu++11 -Wall -I$(my-dir)
endif

MAKE_SO=g++ -o $(1) $(CXXFLAGS) -shared $(2) $(3) -lstdc++ -lregex
SO_EXTENSION=so
